# ── secrets: NEVER commit. Keys live as local dotfiles only. ──
.resend_key
.line_secret
.line_token
.places_key
*.key
.env
.env.*
.dev.vars
# ── junk / heavy ──
node_modules/
.wrangler/
.DS_Store
*.log
# ── build output ──
dist/
*.tsbuildinfo
out/
.scout_key
